import React from "react";
import MoviePage from "../../UI/MoviePage";
interface Props {}

const Upcoming: React.FC<Props> = () => {
  return <MoviePage name="upcoming" />;
};

export default Upcoming;
